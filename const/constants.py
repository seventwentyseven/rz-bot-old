import cmyui

#Don't touch it. I'm changing it on updates, it's also (will be) used for updater
version = cmyui.Version(0, 0, 3)

#Don't touch this too, it's mysql connection stuff
db = cmyui.AsyncSQLPool()